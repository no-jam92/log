---
import '../styles/tokens.css';
---

<html lang="ko">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="generator" content={Astro.generator} />
		<title>Tokens smoke test — Quiet Study</title>
	</head>
	<body>
		<main>
			<p class="label">DESIGN TOKENS · SMOKE TEST</p>
			<h1>조용한 서재</h1>
			<p class="slogan"><em>Crafting quiet, resilient digital experiences</em></p>
			<p class="body">본문 텍스트 — Pretendard 16px / line-height 1.75. 디자인 토큰이 정상 적용되면 라이트 모드에서 따뜻한 오프화이트 배경(<code>--background</code>)에 차콜 텍스트(<code>--on-background</code>)가 보입니다.</p>
			<p class="meta">2026.05.28 · 1 min read</p>
		</main>

		<style>
			body {
				margin: 0;
				background: var(--background);
				color: var(--on-background);
				font-family: var(--body-family);
				font-size: var(--body-size);
				line-height: var(--body-leading);
			}
			main {
				max-width: var(--max-width-content);
				margin: 0 auto;
				padding: var(--section-gap) var(--margin-sm);
			}
			.label {
				font-family: var(--label-family);
				font-size: var(--label-size);
				font-weight: var(--label-weight);
				line-height: var(--label-leading);
				letter-spacing: var(--label-tracking);
				text-transform: uppercase;
				color: var(--on-surface-variant);
				margin-bottom: var(--space-3);
			}
			h1 {
				font-family: var(--h1-family);
				font-size: var(--h1-mobile-size);
				font-weight: var(--h1-weight);
				line-height: var(--h1-leading);
				letter-spacing: var(--h1-tracking);
				margin: 0 0 var(--space-3);
			}
			@media (min-width: 640px) {
				h1 { font-size: var(--h1-size); }
			}
			.slogan {
				font-family: var(--slogan-italic-family);
				font-size: var(--slogan-italic-size);
				font-style: var(--slogan-italic-style);
				font-weight: var(--slogan-italic-weight);
				color: var(--on-surface-variant);
				margin: 0 0 var(--space-4);
			}
			.body { margin: 0 0 var(--space-3); }
			code {
				font-family: var(--font-family-mono);
				font-size: 0.875em;
				background: var(--surface-container);
				padding: 0 4px;
			}
			.meta {
				font-family: var(--meta-family);
				font-size: var(--meta-size);
				line-height: var(--meta-leading);
				color: var(--on-surface-variant);
				margin: var(--space-4) 0 0;
				padding-top: var(--space-3);
				border-top: var(--border-hairline) solid var(--outline-variant);
			}
		</style>
	</body>
</html>
